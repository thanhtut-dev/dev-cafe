<?xml version="1.0"?>
<odoo>

    <record id="loyalty_program_view_form_view_inherit" model="ir.ui.view">
        <field name="name">loyalty.program.form.inherit</field>
        <field name="model">loyalty.program</field>
        <field name="inherit_id" ref="loyalty.loyalty_program_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group/group[2]/field[@name='company_id']" position="before">
                <field name="break_discount" />
            </xpath>
        </field>
    </record>

</odoo>
